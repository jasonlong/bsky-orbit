name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: Build binaries
        run: |
          LDFLAGS="-s -w"
          GOOS=darwin GOARCH=arm64 go build -ldflags="$LDFLAGS" -o bsky-orbit-darwin-arm64 .
          GOOS=darwin GOARCH=amd64 go build -ldflags="$LDFLAGS" -o bsky-orbit-darwin-amd64 .
          GOOS=linux GOARCH=amd64 go build -ldflags="$LDFLAGS" -o bsky-orbit-linux-amd64 .
          GOOS=linux GOARCH=arm64 go build -ldflags="$LDFLAGS" -o bsky-orbit-linux-arm64 .
          GOOS=windows GOARCH=amd64 go build -ldflags="$LDFLAGS" -o bsky-orbit-windows-amd64.exe .

      - name: Create release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            bsky-orbit-darwin-arm64
            bsky-orbit-darwin-amd64
            bsky-orbit-linux-amd64
            bsky-orbit-linux-arm64
            bsky-orbit-windows-amd64.exe
          generate_release_notes: true
